# About Project

## Features

Login

Register

Logout

CRUD Tasks

## Technologies

Python

HTML

Javascript

## Frameworks

FastAPI = Backend

Bootstrap = Frontend

`git clone https://github.com/alexsandersautier/todo-fastapi-html.git `

`python -m venv venv`

`venv/Scripts/activate`

`pip install -r requirements.txt`

`cd client`

`waitress-serve --host=127.0.0.1 --port=5501 server:app`

open new terminal

`fastapi dev controller.py`
